---
interface Props {
    name: string;
    id: string;
}

const { name, id } = Astro.props;
---

<button id={id}class="border-0 border-black sm:text-[1rem] text-[0.8rem] px-3 py-1 z-10">{name}</button>

---
interface Props {
    name: string;
    id: string;
}

const { name, id } = Astro.props;
---

<button id={id} class="border-2 border-black rounded-md px-5 py-1 hover:bg-black hover:text-white z-10">{name}</button>